import styled from "@emotion/styled";

const Container = styled("div")(() => {
  return {
    height: "100vh",
    minHeight: "100vh",
    background: "#c3c3c3",
  };
});

export { Container };
