import { styled } from "@mui/system";

const Container = styled("div")(() => {
  return {
    height: "40vh",
    background: "#000",
    color: "#fff",
  };
});

export default Container;
